// require(filepath.php) throws a FATAL ERROR and EXITS execution DIES
// include(filepath.php) throws a WARNING and CONTINUES  to execute the following lines

// include() inserts the code in the file as many times as you can call it
// include works as printing a component in angular is like bringing a component
// include_one() now you can only include the contents of the file ones